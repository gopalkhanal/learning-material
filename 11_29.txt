merge changes in svn 

-- pull the repo code from the browser 

   and make your change but keep your backup. 

commiting in svn is same as git 

conflict situations in svn. know briefly 

1. totally conflict situation -- take a backup -- pull the latest- make changes on top of it. 
    need to mark as resolved .
2. conflict is ipmple so merge it. need to mark as resolved . 
3. working on same file--- svn was able to resolve itself. 

